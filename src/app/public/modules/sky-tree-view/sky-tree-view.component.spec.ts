
describe('tree view', () => {

  describe('toolbar', () => {
    it('should show toolbar when showToolbar property is set to true', () => {
      // xxx
    });

    it('should hide toolbar when showToolbar property is set to false', () => {
      // xxx
    });

    it('should hide select all / clear all buttons when the tree view does not have checkboxes enabled', () => {
      // xxx
    });

    it('should show select all / clear all buttons when the tree view has checkboxes enabled', () => {
      // xxx
    });

    it('should select all checkboxes when select all is clicked', () => {
      // xxx
    });

    it('should clear all checkboxes when clear all is clicked', () => {
      // xxx
    });

    it('should send proper commands to API when expand all / collapse all buttons are clicked', () => {
      // xxx
    });
  });

  describe('select mode', () => {

    it('should show sky-checkboxes when useCheckbox is enabled', () => {
      // xxx
    });

    it('should show a checked sky-checkbox when nodes are selected programatically', () => {
      // xxx
    });

    it('should show indeterminate state when parent checkboxes are partially selected in cascade mode', () => {
      // xxx
    });

    it('should select nodes when node content is clicked', () => {
      // xxx
    });

    it('should show selected class when node is selected', () => {
      // xxx
    });

    it('should hide and prevent parent node selection when options.leafNodeSelectionOnly is true', () => {
      // xxx
    });

    it('should show and allow parent node selection when options.leafNodeSelectionOnly is not set', () => {
      // xxx
    });

    it('should show isActive class when selected node is inside a split-view container', () => {
      // xxx
    });
  });

  it('should throw an error if the node wrapper component is not wrapped with a sky-tree-view component', () => {
    // xxx
  });

  it('should xxx', () => {
    // xxx
  });
});
